/* For moving the toolbar up
.CTATTutor {
    display: block;
} */

/* Yun set the following height so that it's close to the Spring 20 version (144px), rathern than the current 176px */
.CTATTools {
    /*    margin-top: 10px;*/
    padding: 1px;
    padding-top: 0px;
    height: 158px;
}

.CTATButtons {
    display: flex;
    flex-direction: column;
    /* if comment below, the height will be set to the one set in CTAT.css */
    height: auto;
    /* don't set width auto, because it will grow too*/
}

.CTATTools .CTATHints,
.CTATTools .CTATSkills {
    display: flex;
    flex-direction: column;
    /* flex:1 <==> flex-grow: 1, flex-shrink: 1, flex-basis: 0%*/
    flex: 1;
}

/* detector message window */
.CTATTools .CTATTextField {
    padding: 5px;
    border: 1px solid #ccc;
    border-radius: 5px;
    background-color: #fafafa;
    margin: 1px;
}

.CTATHintWindow,
.CTATSkillWindow {
    /* flex: auto <==> flex: 1 1 auto*/
    flex: auto;
    height: auto;
    width: auto;
    margin: 2px;
    /*    necessary to avoid hint window exceeding the height of CTATTools*/
    min-height: 0;
}

/*hint content*/
.CTATHintWindow--hint-content {
    padding-top: 1px;
}

.CTATDoneButton,
.CTATHintButton,
.CTATButton {
    /* Yun commented below after Octav
    height: 55px;
    width: 55px; */
    height: 50px;
    width: 56px;
}

/* Reflect button */
.CTATButton {
    /* font: 15px Helvetica Neue, Helvetica, Arial, sans-serif; */
    visibility: hidden;
    /* Yun added this part */
    font: 14px Helvetica Neue, Helvetica, Arial, sans-serif;
}

/* This matches to button tag automatically generated by CTAT for reflect button */
.CTAT-button {
    /* border: 1px solid #336699;*/
    border: 1px solid #C70039;
    border-radius: 5px;
    padding: 0;
    /* background-color: #b3d9ff;*/
    background-color: #FF7733;
}

/* Reflect button (latest) */
#reflect_button{
    visibility: hidden;
    font: 14px Helvetica Neue, Helvetica, Arial, sans-serif;
    border: 1px solid #C70039;
    border-radius: 5px;
    padding: 0;
    padding-top: 2px;
    padding-bottom: 2px;
    background-color: #FF7733;
}

.CTATDoneButton {
    order: 2;
}

.CTAT-hint-button--icon,
.CTAT-done-button--icon,
.CTAT-button--icon, #reflect_button_icon {
    /* Yun commented below after Octav
    top: -2px;
    font-size: 35px; */
    top: 2px;
    font-size: 25px;
    margin-bottom: -2px;
}

/*Yun added this after Octav*/
/* .CTAT-done-button--icon {
    top: 0px;
} */

.CTAT-button--icon, #reflect_button_icon {
    font-weight: bold;
}

/* <start> Colors for detector messages */
.idle {
    /* bright blue */
    color: #337DFF;
    font-weight: bold;
}

.hint_avoidance {
    /* orange */
    color: orange;
    font-weight: bold;
}

.hint_abuse {
    /* bright purple */
    color: #E269FF;
    font-weight: bold;
}

.help_after_attempt,
.help_when_confused,
.stay_focused,
.many_problems,
.first_right,
.better_skills {
    /* bright green; if use #8c4 then it's the same as Done */
    color: #49C618;
    font-weight: bold;
}

.reflect, .monitor {
    /* orange red */
    /*    color: #FF5733;*/
    color: #FF7733;
    font-weight: bold;
}

/*https://css-tricks.com/adding-stroke-to-web-text/*/
@supports (-webkit-text-stroke: 0.3px black) {

    .idle,
    .hint_avoidance,
    .hint_abuse,
    .help_after_attempt,
    .help_when_confused,
    .stay_focused,
    .many_problems,
    .first_right,
    .better_skills,
    .reflect,
    .monitor {
        -webkit-text-stroke: 0.3px black;
    }
}

/* <end> Colors for detector messages */


/* For trying to shrink toolbar in landscape mode, but couldn't easily make CTATButtons shrink yet
min-width: 1024px should work on iPad (1024px), but exclude blue margins in the window (10px each side) just in case; min-width: Xpx means the style applies when width >= Xpx 
@media only screen and (min-width: 700px) {
    .CTATTools {
        flex: 0 1 auto;
        min-height: 0;
    }
    .CTATDoneButton,
    .CTATHintButton,
    .CTATButton {
        height: 40px;
    }
    .CTAT-hint-button--icon,
    .CTAT-done-button--icon,
    .CTAT-button--icon {
        top: -2px;
        font-size: 25px;
    }
}
*/
